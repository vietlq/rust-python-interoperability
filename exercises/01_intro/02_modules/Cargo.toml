[package]
name = "modules"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
# If you do not specify [lib.name] then it will take [package.name] which is "modules"
# name = "name_of_your_rust_library"

[dependencies]
pyo3 = { workspace = true }
