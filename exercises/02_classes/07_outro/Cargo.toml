[package]
name = "outro2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = { version = "0.4.42", features = ["serde"] }
pyo3 = { workspace = true, features = ["extension-module", "chrono"] }
